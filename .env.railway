# Railway Environment Configuration for VPN Server
# Copy these to Railway environment variables

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
VPN_API_PORT=8443                  # HTTP API port
VPN_LISTEN_PORT=51820               # WireGuard UDP port
VPN_INTERFACE=wg0                   # WireGuard interface name

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
VPN_SERVER_IP=10.0.0.1/24           # VPN server IP with CIDR
VPN_IPAM_CIDR=10.0.0.0/24           # IP allocation range
VPN_IPAM_GATEWAY=10.0.0.1           # Gateway IP
VPN_CLIENT_IP_DEMO=10.0.0.100       # Demo client IP for registration

# =============================================================================
# TIMEOUT CONFIGURATION (Optional - uses sensible defaults)
# =============================================================================
# VPN_HTTP_READ_TIMEOUT=15s         # HTTP read timeout
# VPN_HTTP_WRITE_TIMEOUT=15s        # HTTP write timeout  
# VPN_HTTP_IDLE_TIMEOUT=60s         # HTTP idle timeout
# VPN_SHUTDOWN_TIMEOUT=10s          # Graceful shutdown timeout

# =============================================================================
# TEST CONFIGURATION (Optional)
# =============================================================================
# VPN_TEST_PEER_PUBKEY=your_test_client_public_key_here
# VPN_TEST_PEER_IP=10.0.0.2         # Test peer IP
# VPN_TEST_INTERFACE=wg-test         # Test interface name
# VPN_TEST_CONTEXT_TIMEOUT=30s      # Test context timeout

# =============================================================================
# RAILWAY AUTOMATIC VARIABLES (provided by Railway)
# =============================================================================
# PORT                              # Railway's internal port (don't override VPN_API_PORT)
# RAILWAY_ENVIRONMENT               # production/staging/development  
# RAILWAY_PROJECT_NAME              # Project name for logging
# RAILWAY_SERVICE_NAME              # Service name for logging